"use client";

import { MemberAddForm } from "@/components/table/_index/components";

export default function Debug() {
  return (
    <div>
      <MemberAddForm onConfirm={() => {}} onCancel={() => {}} />
    </div>
  );
}
